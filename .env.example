# Development Server Port
# Each git worktree can specify a unique port to avoid conflicts
# If not specified, a port is auto-generated based on the worktree path
# DEV_PORT=4322

# Deploy Target (local | github-pages | cloudflare-pages)
# DEPLOY_TARGET=local
