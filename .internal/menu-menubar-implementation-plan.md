# Menu and Menubar パターン実装計画書

> 作成日: 2026-01-10
> 更新日: 2026-01-10（Codex レビュー反映）
> APG Reference: https://www.w3.org/WAI/ARIA/apg/patterns/menubar/

---

## 1. 概要

Menu and Menubar は、アプリケーションスタイルのナビゲーションやアクション選択を提供するパターン。Menubar は水平に配置された常時表示のメニューバーで、各メニュー項目からドロップダウンメニューやサブメニューを開くことができる。

**主要機能**:
- 水平配置の Menubar（常時表示）
- 垂直配置の Menu（ドロップダウン）
- ネストしたサブメニューのサポート
- menuitemcheckbox / menuitemradio によるトグル可能なメニュー項目
- 方向キーによる階層的なナビゲーション

**類似パターン**: menu-button（既存実装）

**複雑度**: 高

---

## 2. APG 仕様サマリー

### Roles

| Role | 対象要素 | 説明 |
|------|----------|------|
| `menubar` | 水平コンテナ (`<ul>`) | 常時表示のトップレベルメニューバー |
| `menu` | 垂直コンテナ (`<ul>`) | ドロップダウン/サブメニュー |
| `menuitem` | メニュー項目 | 標準的なアクション項目 |
| `menuitemcheckbox` | チェック可能項目 | トグル可能なチェックボックス型項目 |
| `menuitemradio` | ラジオ項目 | グループ内で1つのみ選択可能 |
| `separator` | 区切り線 | メニュー項目のグループ分け（非フォーカス可能） |
| `group` | グループコンテナ | menuitemradio のグループ化 + ラベル付け |
| `none` / `presentation` | `<li>` 要素 | リストセマンティクスを SR から隠す |

### Properties（静的属性）

| 属性 | 対象要素 | 値 | 必須 | 備考 |
|------|----------|-----|------|------|
| `aria-haspopup` | サブメニューを持つ menuitem | `"menu"` | Yes* | **`"menu"` を明示的に使用**（`true` は `"menu"` と同等だが、明示的な値が推奨） |
| `aria-labelledby` | menu / menubar | ID参照 | Yes** | アクセシブルな名前 |
| `aria-labelledby` | サブメニュー (`menu`) | 親 menuitem の ID | Yes | **サブメニューは親 menuitem を参照** |
| `aria-label` | menu / menubar | 文字列 | Yes** | labelledby の代替（サブメニューでは非推奨） |
| `aria-orientation` | menubar | `"horizontal"` | No | デフォルトが horizontal |
| `aria-orientation` | menu | `"vertical"` | No | デフォルトが vertical |
| `role="none"` | `<li>` 要素 | - | Yes | リストセマンティクスを隠す |

> \* サブメニューを持つ menuitem のみ必須
> \** aria-labelledby または aria-label のいずれかが必須（サブメニューは labelledby 推奨）

### States（動的属性）

| 属性 | 対象要素 | 値 | 必須 | 変更トリガー |
|------|----------|-----|------|--------------|
| `aria-expanded` | サブメニューを持つ menuitem | `true`/`false` | Yes* | サブメニューの開閉時 |
| `aria-checked` | menuitemcheckbox | `true`/`false` | Yes | トグル時 |
| `aria-checked` | menuitemradio | `true`/`false` | Yes | 選択時 |
| `aria-disabled` | 任意の menuitem* | `true` | No | 無効状態の項目 |
| `aria-hidden` | 閉じたメニュー | `"true"` | Yes | メニュー非表示時に SR からも隠す（`hidden` 属性はセマンティクスを持つため `aria-hidden` を優先） |

> \* サブメニューを持つ menuitem のみ必須

### Keyboard Support

#### Menubar（水平）でフォーカス時

| キー | アクション | menu-button との違い |
|------|------------|---------------------|
| `Enter` | サブメニューを開き、最初の項目にフォーカス。サブメニューがない場合はアクティベート | menu-button ではボタンにフォーカス時に Enter でメニューを開く。menubar では menubar item にフォーカス時に Enter でサブメニューを開く |
| `Space` | Enter と同じ動作 | menu-button と同じトリガー動作だが、対象が menubar item |
| `→` (ArrowRight) | 次の menubar 項目にフォーカス移動（ループ） | **新規**: menu-button にはない水平ナビゲーション |
| `←` (ArrowLeft) | 前の menubar 項目にフォーカス移動（ループ） | **新規**: 水平ナビゲーション |
| `↓` (ArrowDown) | サブメニューを開き、最初の項目にフォーカス | menu-button ではボタンで ArrowDown を押すとメニューを開く。menubar item でも同じ動作 |
| `↑` (ArrowUp) | サブメニューを開き、最後の項目にフォーカス（オプション） | menu-button と同じ動作（最後の項目にフォーカス） |
| `Home` | 最初の menubar 項目にフォーカス | **新規**: menubar 内ナビゲーション |
| `End` | 最後の menubar 項目にフォーカス | **新規**: menubar 内ナビゲーション |
| `Escape` | メニューを閉じる（該当する場合） | menu-button では Escape でメニューを閉じてボタンへ復帰。menubar では Escape でメニューを閉じて該当 menubar item へ復帰 |
| `Tab` / `Shift+Tab` | menubar からフォーカスアウト、全メニューを閉じる | **新規**: menubar 全体からの脱出 |
| 文字キー | タイプアヘッド検索（separator/disabled をスキップ）。**500ms 後にリセット** | menu-button と同じ動作 |

#### Menu（垂直、ドロップダウン/サブメニュー）でフォーカス時

| キー | アクション | menu-button との違い |
|------|------------|---------------------|
| `Enter` | **menuitem**: アクティベートしてメニューを閉じる。**サブメニュー持ち**: サブメニューを開く。**checkbox/radio**: 状態をトグル（**メニューは閉じない**） | **checkbox/radio は menu-button にない**。状態更新後もメニューは開いたまま |
| `Space` | Enter と同じ動作 | checkbox/radio のトグル動作が追加 |
| `↓` (ArrowDown) | 次の項目にフォーカス（ループ） | menu-button のメニュー内では ArrowDown で項目移動。menubar のドロップダウンメニュー内でも同じ挙動 |
| `↑` (ArrowUp) | 前の項目にフォーカス（ループ） | menu-button と同じ挙動 |
| `→` (ArrowRight) | **サブメニューがある場合**: 開いて最初の項目にフォーカス。**Menubar のサブメニュー内**: 現在のメニューを閉じ、次の menubar 項目のサブメニューを開く | **新規**: 階層ナビゲーション。menu-button には存在しない |
| `←` (ArrowLeft) | **サブメニュー内**: サブメニューを閉じ、親の menuitem にフォーカス。**Menubar の直下メニュー内**: メニューを閉じ、前の menubar 項目にフォーカス | **新規**: 階層ナビゲーション。menu-button には存在しない |
| `Home` | 最初の項目にフォーカス | menu-button と同じ挙動 |
| `End` | 最後の項目にフォーカス | menu-button と同じ挙動 |
| `Escape` | メニューを閉じ、親の menuitem（または menubar 項目）にフォーカス | menu-button では Escape でメニューを閉じてボタンへ復帰。menubar では親階層へ復帰 |
| `Tab` | 全メニューを閉じ、次のフォーカス可能要素へ | menu-button と同じ挙動 |
| 文字キー | タイプアヘッド検索（separator/disabled をスキップ）。**500ms 後にリセット** | menu-button と同じ挙動 |

### Focus Management

1. **Roving tabindex**: menubar / menu 内で1つの項目のみ `tabIndex="0"`、他は `tabIndex="-1"`
2. **Menubar 初期フォーカス**: 最初の menubar 項目が `tabIndex="0"`
3. **メニューを開いた時**: 最初の有効な項目にフォーカス
4. **メニューを閉じた時**: 呼び出し元（menubar 項目または親 menuitem）にフォーカスを戻す
5. **disabled 項目**: **フォーカス可能だがアクティベートできない**（APG 推奨に従う）
6. **separator**: フォーカス不可・操作不可
7. **メニュー非表示時**: `aria-hidden="true"` を設定し、SR からも隠す（`hidden` 属性はセマンティクスを持つため `aria-hidden` を優先）

### Disabled 項目のフォーカス方針（明文化）

> **方針**: disabled 項目は**フォーカス可能**とする。
>
> **理由**:
> - APG 公式例では disabled 項目にフォーカスが移動する
> - ユーザーが「その項目が存在するが使用不可」であることを認識できる
> - キーボードナビゲーションで「穴」が開かない
>
> **実装**:
> - `aria-disabled="true"` を設定
> - `tabIndex="-1"` を維持（roving tabindex の対象）
> - Enter/Space でアクティベートしない
> - タイプアヘッド検索ではスキップする

---

## 3. 類似パターンとの差分

> **目的**: 実装時の「menu-button と同じ」という誤解を防ぎ、正確な実装を担保する。

### 類似パターン: menu-button

**選定理由**: menu-button は「ボタンからメニューを開く」という基本構造を共有。しかし menubar は「常時表示の水平メニューバー」「サブメニューの階層」「checkbox/radio 項目」など多くの追加機能を持つ。

### 差分一覧

| 機能 | menu-button | Menu and Menubar | 違いの理由 |
|------|-------------|------------------|------------|
| **トップレベル構造** | `<button>` がトリガー | `<ul role="menubar">` が常時表示 | menubar は常に表示されるナビゲーション要素 |
| **水平ナビゲーション** | なし（ボタンは単一） | `←`/`→` で menubar 項目間を移動 | menubar 内に複数の項目が存在 |
| **サブメニュー** | なし（1階層のみ） | `→` でサブメニューを開く、`←` で閉じる | 階層的なメニュー構造のサポート |
| **`→` キー（menu 内）** | なし | サブメニューを開く or 次の menubar 項目へ | 階層ナビゲーションが必要 |
| **`←` キー（menu 内）** | なし | サブメニューを閉じる or 前の menubar 項目へ | 親階層への戻りが必要 |
| **menuitemcheckbox** | なし | `aria-checked` でトグル状態を管理、**メニューは閉じない** | チェック可能なオプションのサポート |
| **menuitemradio** | なし | グループ内で1つのみ `aria-checked="true"`、**メニューは閉じない** | 排他的選択のサポート |
| **separator** | なし（または簡易的） | `role="separator"` でグループ分け | メニュー項目の論理的グループ化 |
| **`li` の role** | 未使用または暗黙的 | `role="none"` 必須 | リストセマンティクスを SR から隠す |
| **サブメニューのラベル** | N/A | 親 menuitem を `aria-labelledby` で参照 | サブメニューのアクセシブルな名前 |
| **ポインタ操作** | クリックでメニュー開閉 | ホバーで隣接メニューに自動切替 | デスクトップアプリのメニューバー挙動 |

### 差分の詳細説明

#### 差分1: 水平ナビゲーション（Menubar）

**menu-button での動作**:
- ボタンは単一要素のため、水平ナビゲーションは不要
- `←`/`→` キーは使用されない

**Menu and Menubar での動作**:
- Menubar 内で `←`/`→` キーで項目間を移動
- フォーカスはループする（最後から最初へ、最初から最後へ）
- Home/End で最初/最後の項目にジャンプ

**違いが生じる理由**:
- Menubar は複数のトップレベル項目を持つ水平配置のコンテナ
- デスクトップアプリケーションの標準的なメニューバーを模倣

#### 差分2: サブメニューの階層ナビゲーション

**menu-button での動作**:
- メニューは1階層のみ
- `→` キーは使用されない

**Menu and Menubar での動作**:
- `→` キー: サブメニューを持つ項目で押すと、サブメニューを開き最初の項目にフォーカス
- `←` キー: サブメニュー内で押すと、サブメニューを閉じて親 menuitem にフォーカス
- Menubar 直下のメニュー内で `→`/`←`: 現在のメニューを閉じ、隣の menubar 項目のメニューを開く

**違いが生じる理由**:
- 複雑なアプリケーションでは深い階層のメニューが必要
- キーボードだけで階層を行き来できる必要がある

#### 差分3: menuitemcheckbox と menuitemradio

**menu-button での動作**:
- menuitem のみ（アクションを実行するのみ）
- 状態を持たない
- Enter/Space で項目をアクティベートし、メニューを閉じる

**Menu and Menubar での動作**:
- `menuitemcheckbox`: `Space` または `Enter` で `aria-checked` をトグル、**メニューは開いたまま**
- `menuitemradio`: 同じグループ内で1つのみ選択可能、他は自動的に解除、**メニューは開いたまま**
- 通常の `menuitem` は従来通りアクティベートしてメニューを閉じる

**違いが生じる理由**:
- 設定やオプションの選択（表示/非表示の切り替えなど）に使用
- 複数の設定を連続で変更できるようにメニューは開いたままにする
- Word や Excel のようなアプリケーションのメニューを模倣

#### 差分4: `li` 要素への `role="none"`

**menu-button での動作**:
- `<ul>` / `<li>` 構造を使用する場合、`role` が未設定のことがある

**Menu and Menubar での動作**:
- `<li>` 要素に `role="none"` または `role="presentation"` を設定
- リストセマンティクスが SR に露出することを防ぐ

**違いが生じる理由**:
- SR が「リストの1/5」のように読み上げることを防ぐ
- menubar/menu のセマンティクスのみを伝える

#### 差分5: ポインタ操作（ホバーでの自動切替）

**menu-button での動作**:
- ボタンをクリックしてメニューを開閉
- ホバーでの動作なし

**Menu and Menubar での動作**:
- menubar 項目をクリックしてメニューを開閉
- メニューが開いている状態で別の menubar 項目にホバーすると、自動的にそちらのメニューに切り替わる

**違いが生じる理由**:
- デスクトップアプリケーションのメニューバーの標準的な挙動
- マウス操作の効率性向上

### 実装時の注意事項

> ⚠️ 以下の点は「menu-button と同じ」ではないため、個別に実装が必要:

1. **水平ナビゲーション**: Menubar 専用の `←`/`→` ハンドリングを実装。menu-button のコードを流用せず新規実装
2. **階層管理**: 現在開いているメニューのスタック（階層）を状態として管理。サブメニューを開く/閉じる際のフォーカス遷移ロジック
3. **`→`/`←` の文脈依存動作**: 現在のフォーカス位置（menubar / menu / submenu）に応じて異なる動作を実装
4. **checkbox/radio 状態管理**: `aria-checked` の更新と、radio グループ内の排他制御。**メニューを閉じない**動作を実装
5. **`li` への `role="none"`**: 全ての `<li>` 要素に設定
6. **サブメニューの `aria-labelledby`**: 親 menuitem の ID を参照
7. **ホバーでのメニュー切替**: メニュー開状態での mouseenter イベント処理

### 差分確認チェックリスト

実装前に以下を確認:

- [x] 各差分の「違いの理由」が明確に説明されているか
- [x] 「同様」という曖昧な表現を具体的な記述に置換したか
- [x] 実装者が「これは menu-button とは別物」と認識できるか
- [x] checkbox/radio の「メニューを閉じない」動作が明記されているか
- [x] `role="none"` の必要性が明記されているか

---

## 4. テスト設計

DAMP 原則に基づき、以下の構成でテストを作成します。

### テストファイル構成

```typescript
describe('Menubar', () => {
  // 🔴 High Priority: APG 準拠の核心
  describe('APG ARIA Attributes', () => {
    // role の検証
    // aria-* 属性の検証
    // 状態変化の検証
  });

  describe('APG Keyboard Interaction - Menubar', () => {
    // 水平ナビゲーション
    // サブメニュー開閉
  });

  describe('APG Keyboard Interaction - Menu', () => {
    // 垂直ナビゲーション
    // サブメニュー階層ナビゲーション
  });

  describe('APG Keyboard Interaction - Submenu', () => {
    // サブメニュー内のナビゲーション
    // 親メニューへの戻り
  });

  describe('Checkbox and Radio Items', () => {
    // menuitemcheckbox のトグル
    // menuitemradio の排他選択
    // メニューが閉じないことの検証
  });

  describe('Focus Management', () => {
    // Roving tabindex の検証
    // フォーカス移動の検証
    // disabled 項目のフォーカス
  });

  describe('Pointer Interaction', () => {
    // クリックでメニュー開閉
    // ホバーでメニュー切替
  });

  // 🔴 High Priority
  describe('Type-Ahead', () => {
    // 文字検索
    // separator/disabled をスキップ
    // リセットタイミング（500ms後）
  });

  describe('Accessibility', () => {
    // axe-core 違反なしの検証
  });

  // 🟢 Low Priority
  describe('Props & Behavior', () => {
    // コールバック呼び出しの検証
    // className 継承の検証
  });
});
```

### テストケース一覧

#### 🔴 High Priority: ARIA 属性

| テストケース | 検証内容 |
|--------------|----------|
| `has role="menubar" on container` | menubar の role |
| `has role="menu" on dropdown` | ドロップダウンの role |
| `has role="menuitem" on items` | menuitem の role |
| `has role="menuitemcheckbox" on checkbox items` | checkbox 項目の role |
| `has role="menuitemradio" on radio items` | radio 項目の role |
| `has role="separator" on dividers` | separator の role |
| `has role="group" on radio groups` | radio グループの role |
| `has role="none" on li elements` | li のリストセマンティクス除去 |
| `has aria-haspopup="menu" on items with submenu` | サブメニューを持つ項目（`"menu"` のみ） |
| `has aria-expanded on items with submenu` | 展開状態 |
| `updates aria-expanded when submenu opens` | 展開時の更新 |
| `has aria-checked on checkbox items` | checkbox のチェック状態 |
| `has aria-checked on radio items` | radio のチェック状態 |
| `has accessible name on menubar` | menubar のラベル |
| `has accessible name on menu` | menu のラベル |
| `submenu has aria-labelledby referencing parent menuitem` | サブメニューのラベル参照 |
| `closed menu has hidden or aria-hidden` | 非表示メニューの SR 対応 |

#### 🔴 High Priority: キーボード操作（Menubar）

| テストケース | 検証内容 |
|--------------|----------|
| `ArrowRight moves to next menubar item` | 右移動 |
| `ArrowLeft moves to previous menubar item` | 左移動 |
| `ArrowRight wraps from last to first` | 右ループ |
| `ArrowLeft wraps from first to last` | 左ループ |
| `ArrowDown opens submenu and focuses first item` | サブメニュー開く |
| `ArrowUp opens submenu and focuses last item` | サブメニュー開く（最後から） |
| `Enter opens submenu` | Enter でサブメニュー開く |
| `Space opens submenu` | Space でサブメニュー開く |
| `Home moves to first menubar item` | Home キー |
| `End moves to last menubar item` | End キー |
| `Tab moves focus out and closes all menus` | Tab で脱出 |
| `Shift+Tab moves focus out and closes all menus` | Shift+Tab で脱出 |

#### 🔴 High Priority: キーボード操作（Menu）

| テストケース | 検証内容 |
|--------------|----------|
| `ArrowDown moves to next item` | 下移動 |
| `ArrowUp moves to previous item` | 上移動 |
| `ArrowDown wraps from last to first` | 下ループ |
| `ArrowUp wraps from first to last` | 上ループ |
| `ArrowRight opens submenu when item has one` | サブメニューを開く |
| `ArrowRight moves to next menubar item when in top-level menu` | 次の menubar へ |
| `ArrowLeft closes submenu and returns to parent` | サブメニューを閉じる |
| `ArrowLeft moves to previous menubar item when in top-level menu` | 前の menubar へ |
| `Enter activates menuitem and closes menu` | アクティベート |
| `Space activates menuitem and closes menu` | アクティベート |
| `Escape closes menu and returns focus to menubar` | メニューを閉じる |
| `Tab closes all menus and moves focus out` | Tab で脱出 |
| `Home moves to first item` | Home キー |
| `End moves to last item` | End キー |

#### 🔴 High Priority: Checkbox/Radio

| テストケース | 検証内容 |
|--------------|----------|
| `Space toggles menuitemcheckbox` | checkbox トグル |
| `Enter toggles menuitemcheckbox` | checkbox トグル（Enter） |
| `Space on checkbox does not close menu` | **checkbox でメニューは閉じない** |
| `Enter on checkbox does not close menu` | **checkbox でメニューは閉じない** |
| `updates aria-checked on toggle` | aria-checked 更新 |
| `Space selects menuitemradio` | radio 選択 |
| `Space on radio does not close menu` | **radio でメニューは閉じない** |
| `Enter on radio does not close menu` | **radio でメニューは閉じない** |
| `only one radio in group can be checked` | radio 排他制御 |
| `unchecks other radios in group when one is selected` | グループ内排他 |
| `radio group has role="group" with label` | グループのラベル |

#### 🔴 High Priority: フォーカス管理

| テストケース | 検証内容 |
|--------------|----------|
| `first menubar item has tabIndex="0"` | 初期 tabindex |
| `other menubar items have tabIndex="-1"` | 他の tabindex |
| `focused menubar item has tabIndex="0"` | フォーカス時 tabindex |
| `menu items have tabIndex="-1" when menu closed` | メニュー閉時 |
| `first menu item has tabIndex="0" when menu open` | メニュー開時 |
| `separator is not focusable` | separator 非フォーカス |
| `disabled items are focusable` | **disabled 項目はフォーカス可能** |
| `disabled items cannot be activated` | disabled 項目は操作不可 |

#### 🔴 High Priority: ポインタ操作

| テストケース | 検証内容 |
|--------------|----------|
| `click on menubar item opens menu` | クリックでメニュー開く |
| `click on menubar item again closes menu` | クリックでメニュー閉じる |
| `hover on another menubar item switches menu when open` | ホバーでメニュー切替 |
| `click on menuitem activates and closes menu` | 項目クリック |
| `click outside closes menu` | 外部クリック |

#### 🔴 High Priority: タイプアヘッド

| テストケース | 検証内容 |
|--------------|----------|
| `type-ahead focuses matching item` | タイプアヘッド |
| `type-ahead wraps around` | タイプアヘッドループ |
| `type-ahead skips separator` | **separator をスキップ** |
| `type-ahead skips disabled items` | **disabled をスキップ** |
| `type-ahead resets after 500ms` | **リセットタイミング** |

#### 🟡 Medium Priority

| テストケース | 検証内容 |
|--------------|----------|
| `has no axe violations` | WCAG 2.1 AA |
| `has no axe violations with menu open` | メニュー開時 |
| `has no axe violations with submenu open` | サブメニュー開時 |

---

## 5. 実装詳細

### Props 設計（React）

```typescript
// メニュー項目の型
export interface MenuItemBase {
  id: string;
  label: string;
  disabled?: boolean;
}

export interface MenuItemAction extends MenuItemBase {
  type: 'item';
  onSelect?: () => void;
}

export interface MenuItemCheckbox extends MenuItemBase {
  type: 'checkbox';
  checked: boolean;
  onCheckedChange?: (checked: boolean) => void;
}

export interface MenuItemRadio extends MenuItemBase {
  type: 'radio';
  checked: boolean;
  onCheckedChange?: (checked: boolean) => void;
}

export interface MenuItemSubmenu extends MenuItemBase {
  type: 'submenu';
  items: MenuItem[];
}

export interface MenuItemSeparator {
  type: 'separator';
  id: string;
}

export interface MenuItemRadioGroup {
  type: 'radiogroup';
  id: string;
  name: string; // グループ名（状態管理用）
  label: string; // グループのアクセシブルな名前
  items: MenuItemRadio[];
}

export type MenuItem =
  | MenuItemAction
  | MenuItemCheckbox
  | MenuItemRadio
  | MenuItemSubmenu
  | MenuItemSeparator
  | MenuItemRadioGroup;

// Menubar 項目の型
export interface MenubarItem {
  id: string;
  label: string;
  items: MenuItem[];
}

// Menubar コンポーネント props
// aria-label または aria-labelledby のいずれかが必須
type MenubarLabelProps =
  | { 'aria-label': string; 'aria-labelledby'?: never }
  | { 'aria-label'?: never; 'aria-labelledby': string };

export type MenubarProps = Omit<
  React.HTMLAttributes<HTMLUListElement>,
  'role' | 'aria-label' | 'aria-labelledby'
> & {
  items: MenubarItem[];
} & MenubarLabelProps;
```

### フレームワーク別の注意点

#### React
- 複雑な状態管理のため useReducer を検討
- useRef で各メニュー項目への参照を保持
- useEffect でクリックアウトサイド検出
- `li` に `role="none"` を設定

#### Vue
- `defineOptions({ inheritAttrs: false })` の使用
- provide/inject でネストしたサブメニューへのコンテキスト提供
- `v-for` でメニュー項目をレンダリング
- `li` に `role="none"` を設定

#### Svelte
- `$props()` による props 受け取り
- `{...$$restProps}` による属性継承
- store でメニュー状態を管理
- `li` に `role="none"` を設定

#### Astro (Web Components)
- `customElements.define()` でカスタム要素登録
- Shadow DOM またはライト DOM の選択
- イベントデリゲーションでキーボードハンドリング
- `li` に `role="none"` を設定

### 構造図

```
┌─────────────────────────────────────────────────────────────────────┐
│ <ul role="menubar" aria-label="Application menu">                   │
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐   │
│ │ <li         │ │ <li         │ │ <li         │ │ <li         │   │
│ │  role=none> │ │  role=none> │ │  role=none> │ │  role=none> │   │
│ │ ┌─────────┐ │ │ ┌─────────┐ │ │ ┌─────────┐ │ │ ┌─────────┐ │   │
│ │ │ menuitem│ │ │ │ menuitem│ │ │ │ menuitem│ │ │ │ menuitem│ │   │
│ │ │ "File"  │ │ │ │ "Edit"  │ │ │ │ "View"  │ │ │ │ "Help"  │ │   │
│ │ │ tabindex│ │ │ │ tabindex│ │ │ │ tabindex│ │ │ │         │ │   │
│ │ │ =0      │ │ │ │ =-1     │ │ │ │ =-1     │ │ │ │         │ │   │
│ │ │ aria-   │ │ │ │ aria-   │ │ │ │ aria-   │ │ │ │         │ │   │
│ │ │ haspopup│ │ │ │ haspopup│ │ │ │ haspopup│ │ │ │         │ │   │
│ │ │ ="menu" │ │ │ │ ="menu" │ │ │ │ ="menu" │ │ │ │         │ │   │
│ │ │ aria-   │ │ │ │         │ │ │ │         │ │ │ │         │ │   │
│ │ │ expanded│ │ │ │         │ │ │ │         │ │ │ │         │ │   │
│ │ └────┬────┘ │ │ └─────────┘ │ │ └─────────┘ │ │ └─────────┘ │   │
│ └──────┼──────┘ └─────────────┘ └─────────────┘ └─────────────┘   │
│        │                                                            │
│        ▼                                                            │
│ ┌──────────────────────────────┐                                    │
│ │ <ul role="menu"              │                                    │
│ │  aria-labelledby="file-btn"> │  ← 親 menuitem を参照              │
│ │ ┌──────────────────────────┐ │                                    │
│ │ │ <li role="none">         │ │                                    │
│ │ │  <span role="menuitem">  │ │                                    │
│ │ │   New                    │ │                                    │
│ │ └──────────────────────────┘ │                                    │
│ │ ┌──────────────────────────┐ │                                    │
│ │ │ <li role="none">         │ │                                    │
│ │ │  <span role="menuitem"   │─┼──▶ ┌─────────────────────────┐    │
│ │ │   aria-haspopup="menu"   │ │    │ <ul role="menu"         │    │
│ │ │   aria-expanded="false"> │ │    │  aria-labelledby=       │    │
│ │ │   Open                   │ │    │  "open-btn">            │    │
│ │ └──────────────────────────┘ │    │   menuitem "Recent..."  │    │
│ │ ┌──────────────────────────┐ │    │   menuitem "From URL"   │    │
│ │ │ <li role="none">         │ │    └─────────────────────────┘    │
│ │ │  <hr role="separator">   │ │                                    │
│ │ └──────────────────────────┘ │                                    │
│ │ ┌──────────────────────────┐ │                                    │
│ │ │ <li role="none">         │ │                                    │
│ │ │  <span role=             │ │                                    │
│ │ │   "menuitemcheckbox"     │ │                                    │
│ │ │   aria-checked="true">   │ │                                    │
│ │ │   Auto Save              │ │                                    │
│ │ └──────────────────────────┘ │                                    │
│ │ ┌──────────────────────────┐ │                                    │
│ │ │ <ul role="group"         │ │                                    │
│ │ │  aria-label="Theme">     │ │                                    │
│ │ │  <li role="none">        │ │                                    │
│ │ │   <span role=            │ │                                    │
│ │ │    "menuitemradio"       │ │                                    │
│ │ │    aria-checked="true">  │ │                                    │
│ │ │    Light                 │ │                                    │
│ │ │  </li>                   │ │                                    │
│ │ │  <li role="none">        │ │                                    │
│ │ │   <span role=            │ │                                    │
│ │ │    "menuitemradio"       │ │                                    │
│ │ │    aria-checked="false"> │ │                                    │
│ │ │    Dark                  │ │                                    │
│ │ │  </li>                   │ │                                    │
│ │ └──────────────────────────┘ │                                    │
│ └──────────────────────────────┘                                    │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 6. TDD ワークフロー

### Phase 0: 設計準備

1. **APG 仕様の確認** ✅
   - APG 公式ドキュメントを読み込み
   - Examples の動作確認
   - Edge case の洗い出し

2. **類似パターンとの差分明記** ✅
   - セクション3で完了
   - 各機能の「違いの理由」を具体的に記述
   - menu-button との差分が明確

3. **llm.md の作成**
   ```bash
   touch src/patterns/menubar/llm.md
   ```

### Phase 1: テスト作成（RED フェーズ）

1. **React テストの作成**
   ```bash
   touch src/patterns/menubar/Menubar.test.tsx
   npm run test -- Menubar.test.tsx
   ```

2. **他フレームワークのユニットテスト作成**
   - Vue: `Menubar.test.vue.ts`
   - Svelte: `Menubar.test.svelte.ts`
   - Astro: `Menubar.test.astro.ts`

3. **E2E テスト作成**
   - `e2e/menubar.spec.ts`

4. **チェックリスト照合**
   - llm.md と テストの 1:1 対応を確認

### Phase 2: コンポーネント実装

1. **React 実装**
   - `Menubar.tsx` - メインコンポーネント
   - `MenubarDemo.tsx` - デモ用ラッパー

2. **CSS 作成**
   - `src/styles/patterns/menubar.css`
   - `global.css` にインポート追加

3. **他フレームワーク実装**
   - Vue, Svelte, Astro

### Phase 3: リファクタリング

1. **コード整理**
2. **計画-実装-テスト 三点照合**

### Phase 4: ドキュメント作成（英語）

- `AccessibilityDocs.astro`
- `TestingDocs.astro`

### Phase 5: 日本語ドキュメント

- `AccessibilityDocs.ja.astro`
- `TestingDocs.ja.astro`

### Phase 6: ページ作成・統合

- 英語ページ（4フレームワーク × 2ページ = 8ファイル）
- 日本語ページ（4フレームワーク = 4ファイル）
- E2Eテスト用デモページ（4ファイル）
- patterns.ts の更新
- README 更新

---

## 7. Codex レビューポイント

### llm.md レビュー

```
以下の llm.md をレビューしてください:
1. APG 仕様との整合性
2. 必須属性の漏れがないか
3. キーボード操作の網羅性
4. テストチェックリストの妥当性
5. 🔴 記述の明確さ（menu-button との違いが具体的に説明されているか）
```

### テストコードレビュー

```
テストコードをレビューしてください:
1. APG 仕様の全要件がテストされているか
2. DAMP 原則に従っているか
3. Edge case の考慮
4. 🔴 差分項目のテストカバレッジ（水平ナビゲーション、サブメニュー階層、checkbox/radio）
5. 🔴 checkbox/radio で「メニューが閉じない」ことのテスト
```

### 実装コードレビュー

```
実装コードをレビューしてください:
1. APG 仕様への準拠
2. キーボードナビゲーションの正確性
3. 🔴 差分項目の実装確認（menu-button にない機能が正しく実装されているか）
4. 🔴 li への role="none" の設定
5. 🔴 サブメニューの aria-labelledby 設定
```

---

## 8. リスクと注意点

### 技術的リスク

| リスク | 影響度 | 対策 |
|--------|--------|------|
| サブメニューの階層管理の複雑さ | 高 | 状態管理を明確に設計、useReducer 使用 |
| キーボードナビゲーションの文脈依存性 | 高 | 現在位置（menubar/menu/submenu）を明確に追跡 |
| 複数メニュー/サブメニューの同時開閉 | 中 | 開いているメニューのスタックを管理 |
| フォーカス管理の複雑さ | 高 | 階層ごとにフォーカス位置を記憶 |
| メニュー非表示時の SR アクセス | 中 | `hidden` 属性または `aria-hidden="true"` を必ず設定 |
| 水平移動時のメニュー開き直し | 高 | フォーカス/開閉の二重管理を明確に設計 |
| menuitemradio のグルーピング | 中 | `role="group"` + `aria-label` を必ず設定 |

### 実装上の注意点

- **CSS インポート漏れ**: `menubar.css` を作成したら `global.css` にインポート追加
- **Astro Islands 制約**: サブメニューを含む場合はラッパーコンポーネントが必要
- **フォーカス管理**: サブメニュー開閉時のフォーカス遷移に特に注意
- **RTL 対応**: 将来的に `ArrowLeft`/`ArrowRight` の意味が反転する可能性を考慮（現時点では LTR のみ対応）
- **`li` への `role="none"`**: 全ての `<li>` 要素に設定することを忘れない
- **`aria-haspopup`**: 必ず `"menu"` を使用（`true` は使用しない）

### 🔴 計画-実装の乖離を防ぐための注意点

1. **水平ナビゲーション**: Menubar 専用のハンドリングを新規実装（menu-button から流用しない）
2. **`→`/`←` の動作**: 文脈（menubar / menu / submenu）に応じた分岐を明確に実装
3. **checkbox/radio**: menu-button には存在しない機能のため完全新規実装。**メニューを閉じない**動作を忘れない
4. **`role="none"`**: 全ての `<li>` に設定することを確認
5. **サブメニューのラベル**: `aria-labelledby` で親 menuitem を参照

### APG 仕様の曖昧な点

- サブメニューの開閉タイミング（ホバー vs クリック vs キーボード）→ 本実装ではホバーでの自動切替を採用
- 複数のサブメニューを同時に開けるか → 本実装では1つのみ（親階層のメニューは開いたまま）
- menuitemradio のグループ化方法 → 本実装では `role="group"` + `aria-label` を使用

---

## 成果物一覧

### Phase 0: 設計準備
- [ ] APG 仕様の調査・整理
- [ ] `src/patterns/menubar/llm.md`

### Phase 1: テスト作成
- [ ] `Menubar.test.tsx`
- [ ] `Menubar.test.vue.ts`
- [ ] `Menubar.test.svelte.ts`
- [ ] `Menubar.test.astro.ts`
- [ ] `e2e/menubar.spec.ts`

### Phase 2: コンポーネント実装
- [ ] `Menubar.tsx`
- [ ] `Menubar.vue`
- [ ] `Menubar.svelte`
- [ ] `Menubar.astro`
- [ ] `src/styles/patterns/menubar.css`
- [ ] `global.css` にインポート追加

### Phase 3: ドキュメント（英語）
- [ ] `AccessibilityDocs.astro`
- [ ] `TestingDocs.astro`

### Phase 4: 日本語ドキュメント
- [ ] `AccessibilityDocs.ja.astro`
- [ ] `TestingDocs.ja.astro`

### Phase 5: ページ作成
- [ ] `src/pages/patterns/menubar/index.astro`
- [ ] `src/pages/patterns/menubar/react/index.astro`
- [ ] `src/pages/patterns/menubar/vue/index.astro`
- [ ] `src/pages/patterns/menubar/svelte/index.astro`
- [ ] `src/pages/patterns/menubar/astro/index.astro`
- [ ] `src/pages/patterns/menubar/{framework}/demo/index.astro` (×4)
- [ ] `src/pages/ja/patterns/menubar/react/index.astro`
- [ ] `src/pages/ja/patterns/menubar/vue/index.astro`
- [ ] `src/pages/ja/patterns/menubar/svelte/index.astro`
- [ ] `src/pages/ja/patterns/menubar/astro/index.astro`

### Phase 6: 統合
- [ ] `patterns.ts` の更新
- [ ] `README.md` の更新
- [ ] `README.ja.md` の更新
