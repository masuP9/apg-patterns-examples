---
/**
 * Demo-only Page: Tooltip (Astro) - Japanese
 *
 * This page renders the Tooltip component in isolation without
 * the site layout. This ensures clean E2E testing environment.
 */
import '@/styles/global.css';
import Tooltip from '@patterns/tooltip/Tooltip.astro';
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex, nofollow" />
    <title>デモ: ツールチップ (Astro)</title>
  </head>
  <body class="p-8">
    <div class="flex flex-wrap items-center gap-8">
      <Tooltip content="変更を保存">
        <button class="bg-primary text-primary-foreground hover:bg-primary/90 rounded-md px-4 py-2">
          保存
        </button>
      </Tooltip>

      <Tooltip content="右側に表示されます" placement="right">
        <button
          class="bg-secondary text-secondary-foreground hover:bg-secondary/90 rounded-md px-4 py-2"
        >
          右側ツールチップ
        </button>
      </Tooltip>

      <Tooltip content="下側に表示されます" placement="bottom">
        <button
          class="bg-secondary text-secondary-foreground hover:bg-secondary/90 rounded-md px-4 py-2"
        >
          下側ツールチップ
        </button>
      </Tooltip>

      <Tooltip content="このツールチップは無効です" disabled>
        <button class="bg-muted text-muted-foreground rounded-md px-4 py-2">
          無効化されたツールチップ
        </button>
      </Tooltip>
    </div>
  </body>
</html>
