---
import PatternLayout from '../../../../layouts/PatternLayout.astro';
import ToggleButton from '@patterns/button/ToggleButton';
import CodeBlock from '@/components/ui/CodeBlock.astro';
import ExternalLink from '@/components/ui/ExternalLink.astro';
import AccessibilityDocs from '@patterns/button/AccessibilityDocs.astro';
import FrameworkTabs from '@/components/ui/FrameworkTabs.astro';
import { ResponsiveTable } from '@/components/ui/table';
import sourceCode from '@patterns/button/ToggleButton.tsx?raw';
---

<PatternLayout title="Toggle Button - React" pattern="button" framework="react">
  <nav class="mb-6 text-sm">
    <a href="/patterns/" class="text-primary hover:underline">Patterns</a>
    <span class="mx-2 text-muted-foreground">/</span>
    <span>Toggle Button</span>
  </nav>

    <header class="mb-8">
      <h1 class="text-3xl font-bold mb-4">Toggle Button</h1>
      <p class="text-lg text-muted-foreground">
        A two-state button that can be either "pressed" or "not pressed".
      </p>
    </header>

    <!-- Framework Selector -->
    <FrameworkTabs pattern="button" currentFramework="react" />

    <!-- Demo Section -->
    <section class="mb-12">
      <h2 class="text-xl font-semibold mb-4">Demo</h2>
      <div class="p-6 rounded-lg border border-border bg-background">
        <div class="flex flex-wrap gap-4">
          <ToggleButton client:load>
            Mute
          </ToggleButton>
          <ToggleButton client:load initialPressed={true}>
            Dark Mode
          </ToggleButton>
          <ToggleButton client:load disabled>
            Disabled
          </ToggleButton>
        </div>
      </div>
    </section>

    <!-- Accessibility Section -->
    <section class="mb-12">
      <h2 class="text-xl font-semibold mb-4">Accessibility Features</h2>
      <AccessibilityDocs />
    </section>

    <!-- Source Code Section -->
    <section class="mb-12">
      <h2 class="text-xl font-semibold mb-4">Source Code</h2>
      <CodeBlock
        code={sourceCode}
        lang="tsx"
        title="ToggleButton.tsx"
      />
    </section>

    <!-- Usage Section -->
    <section class="mb-12">
      <h2 class="text-xl font-semibold mb-4">Usage</h2>
      <CodeBlock
        code={`import { ToggleButton } from './ToggleButton';

function App() {
  return (
    <ToggleButton
      initialPressed={false}
      onToggle={(pressed) => console.log('Pressed:', pressed)}
    >
      Toggle Me
    </ToggleButton>
  );
}`}
        lang="tsx"
        title="Example"
      />
    </section>

    <!-- API Section -->
    <section class="mb-12">
      <h2 class="text-xl font-semibold mb-4">API</h2>
      <ResponsiveTable>
        <table class="w-full border-collapse">
          <thead>
            <tr class="border-b border-border">
              <th class="text-left py-2 pr-4">Prop</th>
              <th class="text-left py-2 pr-4">Type</th>
              <th class="text-left py-2 pr-4">Default</th>
              <th class="text-left py-2">Description</th>
            </tr>
          </thead>
          <tbody>
            <tr class="border-b border-border">
              <td class="py-2 pr-4"><code>initialPressed</code></td>
              <td class="py-2 pr-4 text-muted-foreground">boolean</td>
              <td class="py-2 pr-4 text-muted-foreground">false</td>
              <td class="py-2">Initial pressed state</td>
            </tr>
            <tr class="border-b border-border">
              <td class="py-2 pr-4"><code>onToggle</code></td>
              <td class="py-2 pr-4 text-muted-foreground">(pressed: boolean) =&gt; void</td>
              <td class="py-2 pr-4 text-muted-foreground">-</td>
              <td class="py-2">Callback when state changes</td>
            </tr>
            <tr class="border-b border-border">
              <td class="py-2 pr-4"><code>children</code></td>
              <td class="py-2 pr-4 text-muted-foreground">ReactNode</td>
              <td class="py-2 pr-4 text-muted-foreground">-</td>
              <td class="py-2">Button label</td>
            </tr>
          </tbody>
        </table>
      </ResponsiveTable>
      <p class="mt-4 text-sm text-muted-foreground">
        All other props are passed to the underlying <code>&lt;button&gt;</code> element.
      </p>
    </section>

    <!-- Resources -->
    <section>
      <h2 class="text-xl font-semibold mb-4">Resources</h2>
      <ul class="list-disc pl-6 space-y-2">
        <li>
          <ExternalLink
            href="https://www.w3.org/WAI/ARIA/apg/patterns/button/"
            class="text-primary hover:underline"
          >
            WAI-ARIA APG: Button Pattern
          </ExternalLink>
        </li>
      </ul>
    </section>
</PatternLayout>
