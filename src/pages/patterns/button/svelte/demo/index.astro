---
/**
 * Demo-only Page: Button (Svelte)
 *
 * This page renders the Button component in isolation without
 * the site layout. This ensures clean E2E testing environment.
 */
import '@/styles/global.css';
import Button from '@patterns/button/Button.svelte';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex, nofollow" />
    <title>Demo: Button (Svelte)</title>
  </head>
  <body class="p-8">
    <div class="flex flex-wrap gap-4">
      <Button client:load onClick={() => alert('Clicked!')}>Click me</Button>
      <Button client:load disabled onClick={() => alert('This should not fire!')}>
        Disabled Button
      </Button>
    </div>
  </body>
</html>
