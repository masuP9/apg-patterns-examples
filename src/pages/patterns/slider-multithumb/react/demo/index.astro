---
/**
 * Demo-only Page: Multi-Thumb Slider (React)
 *
 * This page renders the MultiThumbSlider component in isolation without
 * the site layout. This ensures clean E2E testing environment.
 */
import '@/styles/global.css';
import { MultiThumbSlider } from '@patterns/slider-multithumb/MultiThumbSlider';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="robots" content="noindex, nofollow" />
    <title>Demo: Multi-Thumb Slider (React)</title>
  </head>
  <body class="p-8">
    <div class="flex flex-col gap-8">
      <!-- Basic multi-thumb slider with label -->
      <div data-testid="basic-slider">
        <MultiThumbSlider
          client:load
          defaultValue={[20, 80]}
          label="Price Range"
          format={'${value}'}
          aria-label={['Minimum Price', 'Maximum Price']}
        />
      </div>

      <!-- Disabled slider -->
      <div data-testid="disabled-slider">
        <MultiThumbSlider
          client:load
          defaultValue={[30, 70]}
          label="Disabled Range"
          disabled={true}
          aria-label={['Disabled Min', 'Disabled Max']}
        />
      </div>

      <!-- Vertical slider -->
      <div data-testid="vertical-slider" class="h-48">
        <MultiThumbSlider
          client:load
          defaultValue={[25, 75]}
          label="Vertical Range"
          orientation="vertical"
          aria-label={['Vertical Min', 'Vertical Max']}
        />
      </div>

      <!-- Slider with minDistance -->
      <div data-testid="min-distance-slider">
        <MultiThumbSlider
          client:load
          defaultValue={[40, 60]}
          label="Min Distance Range"
          minDistance={10}
          aria-label={['Distance Min', 'Distance Max']}
        />
      </div>
    </div>
  </body>
</html>
