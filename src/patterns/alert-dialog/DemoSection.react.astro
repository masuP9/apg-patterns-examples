---
import type { Framework } from '@/lib/frameworks';
import type { Locale } from '@/i18n/ui';
import AlertDialogDemoReact from './AlertDialogDemo';

interface Props {
  framework?: Framework;
  locale?: Locale;
}
---

<AlertDialogDemoReact client:only="react" />
