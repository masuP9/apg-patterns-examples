---
import type { Framework } from '@/lib/frameworks';
import type { Locale } from '@/i18n/ui';
import AlertDialogDemoSvelte from './AlertDialogDemo.svelte';

interface Props {
  framework?: Framework;
  locale?: Locale;
}
---

<AlertDialogDemoSvelte client:only="svelte" />
