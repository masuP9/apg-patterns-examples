---
import type { Framework } from '@/lib/frameworks';
import { MultiThumbSlider as MultiThumbSliderReact } from './MultiThumbSlider';

interface Props {
  framework?: Framework;
}
---

<div class="border-border bg-background rounded-lg border p-6">
  <div class="flex flex-col gap-8">
    <div>
      <MultiThumbSliderReact
        client:load
        defaultValue={[20, 80]}
        label="Price Range"
        format={'${value}'}
        aria-label={['Minimum Price', 'Maximum Price']}
      />
    </div>
    <div>
      <MultiThumbSliderReact
        client:load
        defaultValue={[25, 75]}
        label="Temperature Range"
        format={'{value}°C'}
        aria-label={['Minimum Temperature', 'Maximum Temperature']}
      />
    </div>
    <div>
      <MultiThumbSliderReact
        client:load
        defaultValue={[30, 70]}
        min={0}
        max={100}
        step={5}
        minDistance={10}
        label="Budget Range (with minDistance)"
        format={'${value}'}
        aria-label={['Minimum Budget', 'Maximum Budget']}
      />
    </div>
    <div>
      <MultiThumbSliderReact
        client:load
        defaultValue={[40, 60]}
        label="Disabled"
        disabled
        format={'{value}%'}
        aria-label={['Lower Value', 'Upper Value']}
      />
    </div>
  </div>
</div>
