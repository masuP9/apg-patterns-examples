import CodeFigure from '@/components/ui/CodeFigure.astro';

## Overview

Range widgets allow users to set or view a value within a defined range. ARIA defines roles for four types: slider, spinbutton, scrollbar, and meter. Each requires specific properties to communicate the current value and its limits to assistive technology.

## Range Widget Types

| Role           | Purpose                           | User Interaction    |
| -------------- | --------------------------------- | ------------------- |
| **slider**     | Select a value from a range       | Arrow keys, drag    |
| **spinbutton** | Enter/adjust a numeric value      | Arrow keys, type    |
| **scrollbar**  | Control scroll position           | Drag, arrow keys    |
| **meter**      | Display a gauge value (read-only) | None (display only) |

## Required ARIA Properties

### For All Range Widgets

```html
<div
  role="slider"
  aria-valuenow="50"
  aria-valuemin="0"
  aria-valuemax="100"
  aria-label="Volume"
></div>
```

| Property         | Required | Description                              |
| ---------------- | -------- | ---------------------------------------- |
| `aria-valuenow`  | Yes      | Current value                            |
| `aria-valuemin`  | Yes\*    | Minimum value (\*implicit 0 for meter)   |
| `aria-valuemax`  | Yes\*    | Maximum value (\*implicit 100 for meter) |
| `aria-valuetext` | No       | Human-readable value text                |

### aria-valuetext for Non-numeric Values

When the value isn't meaningful as a number:

```html
<div
  role="slider"
  aria-valuenow="120"
  aria-valuemin="0"
  aria-valuemax="480"
  aria-valuetext="2:00 PM"
  aria-label="Meeting start time"
></div>

<div
  role="slider"
  aria-valuenow="2"
  aria-valuemin="1"
  aria-valuemax="5"
  aria-valuetext="Medium priority"
></div>
```

## Native HTML Alternatives

Consider native HTML elements first:

```html
<label for="volume">Volume</label>
<input type="range" id="volume" min="0" max="100" value="50" />

<label for="quantity">Quantity</label>
<input type="number" id="quantity" min="1" max="99" value="1" />

<label for="disk-usage">Disk usage</label>
<meter id="disk-usage" min="0" max="100" low="33" high="66" optimum="20" value="75">75%</meter>

<label for="upload">Upload progress</label>
<progress id="upload" max="100" value="70">70%</progress>
```

## Implementation Tips

### Multi-thumb Sliders

For sliders with multiple thumbs (e.g., price range):

```html
<div role="group" aria-label="Price range">
  <div
    role="slider"
    aria-label="Minimum price"
    aria-valuenow="20"
    aria-valuemin="0"
    aria-valuemax="100"
  ></div>
  <div
    role="slider"
    aria-label="Maximum price"
    aria-valuenow="80"
    aria-valuemin="0"
    aria-valuemax="100"
  ></div>
</div>
```

### Orientation

Specify orientation for vertical sliders:

```html
<div
  role="slider"
  aria-orientation="vertical"
  aria-valuenow="50"
  aria-valuemin="0"
  aria-valuemax="100"
></div>
```

### Read-only Meters

Meters are inherently read-only. Use `meter` role for display-only gauges:

```html
<div
  role="meter"
  aria-valuenow="75"
  aria-valuemin="0"
  aria-valuemax="100"
  aria-label="Battery level"
>
  75%
</div>
```

## Common Pitfalls

### Missing valuetext for Complex Values

<CodeFigure
  code={`<div role="slider" aria-valuenow="50" aria-valuemin="0" aria-valuemax="480" aria-label="Time"></div>`}
  lang="html"
  caption="Bad: Screen reader announces &quot;50&quot; — meaningless as a time value."
/>

<CodeFigure
  code={`<div
  role="slider"
  aria-valuenow="50"
  aria-valuemin="0"
  aria-valuemax="480"
  aria-valuetext="8:30 AM"
  aria-label="Time"
></div>`}
  lang="html"
  caption="Good: Screen reader announces &quot;8:30 AM&quot; — meaningful to users."
/>

### Inconsistent Min/Max/Now

<CodeFigure
  code={`<div role="slider" aria-valuenow="150" aria-valuemin="0" aria-valuemax="100"></div>`}
  lang="html"
  caption="Bad: valuenow (150) is outside the range (0–100)."
/>

<CodeFigure
  code={`<div role="slider" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100"></div>`}
  lang="html"
  caption="Good: valuenow is within the valid range."
/>

### Using Slider for Read-only Display

<CodeFigure
  code={`<div role="slider" aria-valuenow="75" aria-readonly="true"></div>`}
  lang="html"
  caption="Bad: Slider implies interactivity — aria-readonly does not change that expectation."
/>

<CodeFigure
  code={`<div role="meter" aria-valuenow="75"></div>`}
  lang="html"
  caption="Good: Meter role clearly communicates a display-only gauge."
/>
